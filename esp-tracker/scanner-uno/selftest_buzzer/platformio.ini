; Buzzer-only isolation test. No libraries needed at all — the AVR core's
; tone()/noTone() is built in.

[env:buzzer_only]
platform  = atmelavr
board     = uno
framework = arduino
monitor_speed = 115200
build_flags = -Os
