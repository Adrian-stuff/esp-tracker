; EEPROM-only isolation test. No libraries needed — EEPROM is built into
; the AVR core.

[env:eeprom_only]
platform  = atmelavr
board     = uno
framework = arduino
monitor_speed = 115200
build_flags = -Os
