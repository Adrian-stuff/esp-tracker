; RC522-only isolation test — nothing else loaded. Part of the
; per-peripheral split; see ../selftest_sim_only/platformio.ini for why.

[env:rfid_only]
platform  = atmelavr
board     = uno
framework = arduino
monitor_speed = 115200
lib_deps = miguelbalboa/MFRC522@^1.4.11
build_flags = -Os
