; DS1302-only isolation test. Nothing else loaded — part of the
; per-peripheral split; see ../selftest_sim_only/platformio.ini for why.

[env:rtc_only]
platform  = atmelavr
board     = uno
framework = arduino
monitor_speed = 115200
lib_deps = makuna/RTC@^2.5.0
build_flags = -Os
